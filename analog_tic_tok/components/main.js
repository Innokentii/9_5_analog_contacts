import { StyleSheet, Text, View, ImageBackground, TextInput, TouchableWithoutFeedback } from 'react-native';


export default function Main_f() {

    return(
        <View>
            <Text style={{color: 'red'}}>1</Text>
        </View>
    );
}

const styles = StyleSheet.create({

});